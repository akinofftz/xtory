public class StringUtil {
	public static int indexOf(String string, String needle) {
		return string.indexOf(needle);
	}
	public static int lastIndexOf(String string, String needle) {
		return string.lastIndexOf(needle);
	}
}
